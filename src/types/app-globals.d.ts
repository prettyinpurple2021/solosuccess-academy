/**
 * Build-time constants injected by vite.config.ts `define`.
 * Used by the /status page to show the deployed version and build time.
 */
declare const __BUILD_VERSION__: string;
declare const __BUILD_TIME__: string;
